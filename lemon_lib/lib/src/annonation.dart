class Url {
  final String url;
  const Url(this.url);
}

class POST {
  final String url;
  const POST({this.url});
}


class GET {
  final String url;
  const GET({this.url});
}

class Path{
  final String url;
  const Path(this.url);
}

class Controller{
  const Controller();
}

class Headers{
  final String url;
  const Headers({this.url});
}

class Root{
  const Root();
}